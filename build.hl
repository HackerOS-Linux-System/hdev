// cargo
// rustc
// npm

> cd source-code
> npm install
> npm run tauri build
;; install mising >
;; > sudo apt install libgtk-3-dev
;; > sudo apt install libsoup-3.0-dev
;; > sudo apt install libwebkit2gtk-4.1-dev
